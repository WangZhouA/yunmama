<resources>

    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="AppTheme" parent="BaseTheme"></style>

    <!--沉浸式状态栏-->
    <style name="TranslucentTheme" parent="AppTheme"></style>

    <!--全屏显示theme-->
    <style name="FullScreenTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- 登录输入框style -->
    <style name="login_edit_style">
        <item name="android:background">@null</item>
        <item name="android:padding">14dp</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textColorHint">@color/hint_text_color</item>
        <item name="android:textSize">@dimen/text_small</item>
    </style>

    <!--提交按妞style-->
    <style name="submit_btn_style">
        <item name="android:background">@drawable/submit_btn_enable_status_drawable</item>
        <item name="android:textColor">@drawable/submit_btn_text_enbale_status_drawable</item>
        <item name="android:layout_marginRight">15dp</item>
        <item name="android:layout_marginLeft">15dp</item>
        <item name="android:textSize">@dimen/text_small</item>
    </style>

    <!--中心白按妞style-->
    <style name="unsubmit_btn_style">
        <item name="android:background">@drawable/unsubmit_btn_enable_status_drawable</item>
        <item name="android:textColor">@drawable/unsubmit_btn_text_enbale_status_drawable</item>
        <item name="android:layout_marginRight">15dp</item>
        <item name="android:layout_marginLeft">15dp</item>
        <item name="android:textSize">@dimen/text_small</item>
    </style>

    <!--Text style -->
    <style name="text_style">
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">@dimen/text_small</item>
    </style>

    <style name="tabLayoutTextStyle">
        <item name="android:textSize">@dimen/text_main</item>
    </style>
    <!--  防孕吐单选按钮style -->
    <style name="fangyuntu_style">
        <item name="android:button">@null</item>
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:textSize">@dimen/text_small</item>
        <item name="android:background">@drawable/fangyuntu_radio_button_style</item>
        <item name="android:textColor">@drawable/fangyuntu_text_color</item>
        <item name="android:gravity">center</item>
    </style>
    <!-- 胎心仪异常提示框style -->
    <style name="device_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/dialog_transpant_bg</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <!-- 胎心仪历史数据按钮style -->
    <style name="historical_text_style">
        <item name="android:textSize">@dimen/text_main</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:paddingTop">1dp</item>
        <item name="android:paddingBottom">1dp</item>
    </style>
    <!--  选择支付按钮style -->
    <style name="pay_checkbox_style">
        <item name="android:button">@drawable/pay_checkbox_style</item>
        <item name="android:focusable">false</item>
        <item name="android:focusableInTouchMode">false</item>
        <item name="android:clickable">false</item>
    </style>
    <!-- 发帖图片选择 -->
    <style name="ActionMenuTextStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textSize">@dimen/action_text_size</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="PhotoPickerTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/text_black</item><!--设置显示图片的背景-->
        <item name="actionMenuTextAppearance">@style/ActionMenuTextStyle</item>
    </style>
    <!-- 购物选择Sku提示框样式 -->
    <style name="CommonBottomDialogStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/menu_dialog_anim_style</item>
    </style>


    <!--dialog的样式-->
    <style name="Custom_Progress" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@android:color/transparent</item><!--边框-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:windowBackground">@color/transparent40</item><!--背景透明-->
        <item name="android:backgroundDimAmount">0.4</item>
        <item name="android:windowNoTitle">true</item><!--无标题-->
    </style>

    <style name="time_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@color/white</item>
    </style>


    <!-- ActionSheet进出动画 -->
    <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/actionsheet_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/actionsheet_dialog_out</item>
    </style>

    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>

    <style name="MyDialog" parent="@android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>

    </style>
    <!--  购物车编辑按钮style -->
    <style name="cart_checkbox_style">
        <item name="android:button">@drawable/pay_checkbox_style</item>
    </style>
</resources>
